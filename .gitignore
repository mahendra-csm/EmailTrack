/node_modules
/.next
/out
/build
next-env.d.ts

# local data
/data
*.sqlite
*.sqlite-shm
*.sqlite-wal

npm-debug.log*
.DS_Store
tsconfig.tsbuildinfo

# local env (never commit - holds SMTP passwords and DB credentials)
.env*.local
!.env.local.example
